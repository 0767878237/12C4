{
  "name": "my-project",
  "version": "1.0.0",
  
  "scripts": {
    "start": "serve .",
    "build":"mkdir -p public && cp index.html public/ && cp -r css public/ || true"
  },
  "devDependencies": {
    "serve": "^14.0.0"
  }
  
}